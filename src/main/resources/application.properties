#server.port=8081
server.contextPath=/shop

server.port=${PORT:8080}

#management.port=9001
#localhost:9001/health
#localhost:9001/beans




#spring.datasource.url=jdbc:mysql://google/${CLOUD_SQL_DATABASE_NAME}?cloudSqlInstance=${CLOUD_SQL_INSTANCE}&socketFactory=com.google.cloud.sql.mysql.SocketFactory
#spring.datasource.username=${CLOUD_SQL_USERNAME}

#spring.datasource.url=jdbc:postgresql://localhost/shop_home_test
#spring.datasource.username=postgres
#spring.datasource.password=password

#The environment variable JDBC_DATABASE_URL is provided by Heroku and contains the full JDBC string for connecting to the database.
spring.datasource.url=${JDBC_DATABASE_URL}

spring.jpa.generate-ddl=true

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

#If credentials are not specified explicitly – a default username will be used and password will be randomly generated by Boot at startup
#Todo: Look at -- http://www.baeldung.com/security-spring
